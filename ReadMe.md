Server for coordinating up-time of further server-software with other people.

## Setup
* `virtualenv venv`
* `pip install Flask`
* `pip install apscheduler`
* `pip install pexpect`
* `export FLASK_APP=hello.py`
